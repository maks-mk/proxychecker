# 🚀 Sing-box Proxy Checker v5.0 (Local Enterprise)

Высокопроизводительная консольная утилита для массовой проверки прокси-серверов.
Построена на базе ядра **Sing-box** с архитектурой **v5.0**, оптимизированной для Windows и Linux.

Скрипт решает главные проблемы аналогов: зависания UI, конфликты портов (TIME_WAIT) и утечки памяти при проверке больших баз (10k+ строк).

![Python](https://img.shields.io/badge/Python-3.9%2B-blue) ![Sing-box](https://img.shields.io/badge/Core-Sing--box_1.9.0-success) ![Architecture](https://img.shields.io/badge/Architecture-Global_Pool-orange)

---

## 🔥 Ключевые возможности v5.0

### 🧠 Архитектура и Производительность
*   **Global ThreadPool (150-200 потоков):** Единый пул потоков для всех задач (TCP и HTTP). Исключает создание тысяч лишних потоков, которые убивают планировщик ОС.
*   **Smart Port Allocation:** Скрипт **заранее ищет свободные диапазоны портов** перед запуском ядра. Это полностью устраняет ошибки `[WinError 10048] Address already in use`.
*   **Non-blocking UI:** Использование `as_completed` позволяет обновлять прогресс-бар плавно, по мере проверки каждого прокси, а не рывками в конце батча.

### 🛡️ Безопасность и Валидация
*   **IP Leak Check:** Скрипт знает ваш реальный IP. Если прокси "прозрачный" и светит ваш IP — он помечается как **LEAK** и отбрасывается.
*   **TCP Pre-Check:** Мгновенный отсев "мертвых" портов (до 70% мусора) перед запуском тяжелого ядра.
*   **Random uTLS:** Рандомизация отпечатка браузера (`Chrome`, `Firefox`, `iOS`) для обхода простых блокировок.

### 📊 Информативность
*   **GeoIP & Flags:** Автоматическое определение страны и добавление флага в название.
*   **Clean Renaming:** Удаление старых рекламных названий из ссылок.
    *   *Было:* `vless://...@ip:port#Free_Channel_123`
    *   *Стало:* `vless://...@ip:port#🇩🇪 DE | 🚀 125ms | VLESS`

---

## 🛠 Установка

1.  **Требования:**
    *   Установленный **Python 3.8+**.
    *   Интернет (для скачивания ядра при первом запуске).

2.  **Установка библиотек:**
    Откройте терминал/командную строку в папке скрипта и выполните:
    ```bash
    pip install requests tqdm colorama
    ```

---

## 🚀 Запуск

1.  **Подготовка:**
    Положите ваши `.txt` файлы с прокси в папку `keys/`.
    *   *Скрипт найдет все файлы в этой папке автоматически.*
    *   *Поддерживаются ссылки: `vmess://`, `vless://`, `trojan://`, `ss://`, `hy2://`.*

2.  **Запуск:**
    ```bash
    python proxy_checker_v5.py
    ```
    *При первом запуске скрипт сам скачает нужное ядро sing-box для вашей Windows/Linux/macOS.*

3.  **Результат:**
    Рабочие прокси будут сохранены в файл:
    📂 `output/live.txt`

---

## ⚙️ Настройки (Конфигурация)

Вы можете изменить параметры в начале файла скрипта:

```python
# Таймаут ожидания ответа от сайта (сек)
TIMEOUT_SEC = 6

# Проверка на утечку IP (True = строгая анонимность)
CHECK_IP_LEAK = True 

# Количество потоков (уменьшите, если слабый ПК)
GLOBAL_POOL = ThreadPoolExecutor(max_workers=150)

# Размер пачки прокси на один процесс ядра
# Для Windows рекомендуется 8-10, для Linux 40-50
BATCH_SIZE = 8 
```

---

## ❓ Частые вопросы

**В: Почему скорость проверки падает?**
О: Если у вас Windows, система имеет лимит на открытие сокетов. Скрипт v5.0 оптимизирован под это (паузы, семафоры), но "узким местом" может быть ваш роутер или провайдер.

**В: Скрипт пишет "IP Leak", что это?**
О: Это значит, что прокси рабочий, но он не скрывает ваш реальный IP адрес. Для безопасности такие прокси считаются "плохими" и не попадают в `live.txt`.

**В: Поддерживается ли Reality?**
О: Да, полная поддержка `VLESS Reality` и `Vision`.

---

## 📜 Поддерживаемые протоколы

| Протокол | Транспорт | Безопасность |
| :--- | :--- | :--- |
| **VLESS** | TCP, WS, gRPC | TLS, Reality, Vision |
| **VMess** | TCP, WS, gRPC | TLS, None |
| **Trojan** | TCP, WS, gRPC | TLS |
| **Shadowsocks** | TCP | AEAD |
| **Hysteria 2** | UDP | TLS, Obfs |

---
*License: MIT*