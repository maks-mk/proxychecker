# 🛡️ Sing-box Proxy Checker Bot (v5.8 Turbo)

Telegram-бот для скоростной проверки прокси на базе ядра **Sing-box v1.11.4**.
Оптимизирован для работы как на мощных выделенных серверах, так и на слабых VPS (1 vCPU).

![Python](https://img.shields.io/badge/Python-3.9%2B-blue) ![Sing-box](https://img.shields.io/badge/Core-v1.11.4-success)

## 🔥 Ключевые возможности

*   **Smart Deduplication:** Удаляет дубликаты на логическом уровне (Host + Port + Auth), а не просто сравнивая строки.
*   **Link Hygiene:** Очищает ссылки от мусорных параметров (`name`, `spider`, `plugin`) перед проверкой.
*   **Turbo Engine:** Гибридная архитектура (Asyncio + ThreadPool) с пакетной обработкой (Batching) для снижения нагрузки на CPU.
*   **Real-time UX:** Живая статистика в сообщении: скорость (prx/s), время, прогресс-бар и счетчик валида.
*   **Port Rotation:** Менеджер портов для предотвращения коллизий при одновременной работе нескольких пользователей.
*   **TCP Pre-Check:** Быстрый отсев недоступных портов перед запуском ядра.

## 📡 Поддерживаемые протоколы
*   **VLESS** (Reality, Vision, gRPC, WS, TCP)
*   **VMess** (WS, gRPC, TCP)
*   **Trojan**
*   **Shadowsocks** (Legacy & SIP002)
*   **Hysteria 2**

## 🛠 Установка

1.  **Требования:** Python 3.8+
2.  **Зависимости:**
    ```bash
    pip install aiogram aiohttp requests
    ```

## ⚙️ Настройка

### 1. `bot.py`
```python
TOKEN = "YOUR_TOKEN_HERE"   # Ваш Telegram токен

# Лимиты
MAX_FILES_PER_USER = 5      # Файлов в очереди
MAX_LINKS_PER_CHECK = 1000  # Ссылок за раз
BATCH_SIZE = 50             # Размер пачки (50 оптимально для слабых CPU)
```

### 2. `checker_engine.py` (Движок)
```python
TIMEOUT_SEC = 8             # Таймаут на подключение
GLOBAL_POOL = 60            # Кол-во потоков (снизить до 30-50 для слабых VPS)
CHECK_IP_LEAK = True        # Проверка на прозрачность прокси
```

## ▶️ Запуск
```bash
python bot.py
```
*Ядро Sing-box скачивается автоматически под вашу ОС (Windows/Linux/macOS) при первом запуске.*

## 🤖 Использование

1.  Отправьте боту **.txt** файлы с прокси (можно несколько).
2.  Бот автоматически удалит дубликаты и мусор.
3.  Нажмите **🚀 Запустить проверку**.
4.  По завершении бот пришлет файл `live_HH-MM.txt` с рабочими конфигами, отсортированными по пингу.

**Формат вывода:**
```text
vless://uuid@ip:port?...#🇩🇪 DE | 🚀 125ms | VLESS
```

---
*License: MIT*