# 🚀 Sing-box Proxy Checker v6.5 (Smart Deduplication)

Высокопроизводительная консольная утилита для проверки прокси с **умной дедупликацией** и **улучшенной стабильностью**.
Оптимизирована для параллельной проверки больших списков без фризов и утечек системных ресурсов.

![Python](https://img.shields.io/badge/Python-3.8%2B-blue) ![Sing-box](https://img.shields.io/badge/Core-v1.11.4-success) ![Platform](https://img.shields.io/badge/Platform-Win%2FLinux%2FMac-lightgrey)

## 🔥 Ключевые возможности v6.5

*   **Smart Deduplication:** Удаляет только *полные* дубликаты (MD5 хеш конфигурации). Сохраняет разные UUID, Transport (WS/gRPC) и Security (TLS/Reality) на одном сервере — идеально для публичных списков.
*   **Strict Exception Handling:** Полный отказ от "bare except". Конкретная диагностика ошибок парсинга (VMess JSON, Base64, URL).
*   **Windows-Native Stability:** Исправлена проблема "отваливается интернет после проверки". Агрессивный cleanup процессов + защита системных DNS-настроек.
*   **Protocol Support:** Полная поддержка Reality (strict validation), Vision, gRPC, WebSocket, TCP.
*   **GeoIP & Leak Protection:** Определение страны прокси + проверка на прозрачность (IP Leak).
*   **Batch Processing:** Пакетная обработка (50-100 прокси за запуск ядра) с семафором ограничения concurrency.

## 📡 Поддерживаемые протоколы

| Протокол | Features |
|----------|----------|
| **VLESS** | Reality (x25519), Vision, gRPC, WebSocket, HTTP, TCP |
| **VMess** | WebSocket, gRPC, HTTP/2, TCP (alterId auto) |
| **Trojan** | WebSocket, gRPC, TCP |
| **Shadowsocks** | SIP002, Legacy (base64), все методы |
| **Hysteria 2** | TLS, QUIC |

## 🛠 Установка

**Требования:** Python 3.8+, Windows 10/11, Linux или macOS

```bash
# 1. Зависимости
pip install aiohttp requests tqdm colorama

# 2. Подготовка папок
mkdir keys
mkdir output
```

## 🚀 Использование

1.  Скопируйте `.txt` файлы с прокси-ссылками (по одной на строку) в папку **`keys/`**  
    *Поддерживаются: `vmess://`, `vless://`, `trojan://`, `ss://`, `hy2://`, `hysteria2://`*
2.  Запустите:
    ```bash
    python proxy_checker_v6.5.py
    ```
3.  Результат: `output/live.txt` — отсортировано по пингу (возрастание).

## ⚙️ Конфигурация (глобальные переменные)

```python
# Скорость vs Стабильность
BATCH_SIZE = 50          # Windows: 50, Linux/Mac: 100 (зависит от ОС)
CONCURRENCY = 2          # Параллельных батча (не трогать если <16GB RAM)
TIMEOUT_SEC = 10         # Таймаут HTTP-запроса через прокси
STARTUP_TIMEOUT = 15     # Ожидание запуска sing-box ядра

# Функционал
CHECK_IP_LEAK = True     # Требует определения вашего IP перед стартом
PING_URL = "https://cp.cloudflare.com/"  # Endpoint для проверки latency
GEO_URL = "http://ip-api.com/json/"      # Сервис геолокации
```

## 🖥️ Windows-специфика (Важно!)

**Проблема:** На Windows 10/11 после принудительной остановки скрипта (Ctrl+C) может "отвалиться" интернет (сайты не открываются, хотя пинги идут).

**Причина:** Sing-box модифицирует DNS-резолвер. При аварийном завершении настройки не всегда сбрасываются.

**Решение в коде v6.5:**
- `"set_system_proxy": False` — не трогает WinHTTP/IE настройки
- Агрессивный `cleanup_processes()` с `taskkill /F /T` (убивает дерево процессов)
- Авто-сброс DNS после ошибок

**Если интернет всё же пропал:**
Запустите от имени Администратора:
```powershell
ipconfig /flushdns
netsh winsock reset
# Перезагрузка обязательна после winsock reset
```

## 📊 Пример вывода статистики

```text
=================================================== СТАТИСТИКА ===================================================

Загружено строк: 1500
Удалено дублей по конфигурации: 342
Проверено (уник): 1158
Всего живых: 423 (36.5%)

ПРОТОКОЛ      | ЖИВЫЕ    | ВСЕГО    | УСПЕХ %  
--------------------------------------------------
Hysteria2     | 45       | 80       | 56.2%    
Shadowsocks   | 120      | 340      | 35.3%    
Trojan        | 89       | 210      | 42.4%    
VLESS         | 140      | 380      | 36.8%    
VMess         | 29       | 148      | 19.6%    

ТОП ОШИБОК:
 - SSL Error      : 234 (Порт открыт, но TLS handshake failed)
 - Timeout        : 189
 - Conn Error     : 156 (Порт закрыт/хост недоступен)
 - Core Died      : 45 (Невалидный Reality Public Key)
 - No Ports       : 12 (Не хватило локальных портов)

📁 Файл сохранен: output/live.txt
============================================================
```

## 🔍 Логирование и Отладка

При ошибках парсинга детали пишутся в `logger` (уровень INFO). Для детальной отладки:

```python
# В начале файла измените:
logging.basicConfig(level=logging.DEBUG)  # Было INFO
```

Сообщения `logger.debug()` видны только при DEBUG уровне и содержат причины отбраковки конкретных ссылок (например, "VMess parse error: Expecting property name enclosed in double quotes").

## 🛡️ Безопасность

- **No System Proxy:** Скрипт никогда не устанавливает системный прокси (только sing-box core в режиме inbound).
- **Checksum Validation:** Sing-box бинарник проверяется на целостность при скачивании (в v6.6 планируется).
- **Temp Files:** Конфиги sing-box создаются в `%TEMP%` (Windows) или `/tmp` и удаляются даже при Crash.

---
*License: MIT | Author: Senior Dev Contribution*
```

**Ключевые изменения в документации:**
1. **Версия:** Обновлено до v6.5 (Smart Deduplication)
2. **Windows Section:** Добавлен раздел о проблеме с интернетом и её решении
3. **Deduplication:** Описана новая логика (MD5 хеш конфига вместо host:port)
4. **Exceptions:** Упомянута strict exception handling
5. **Stats:** Обновлен пример вывода под новый формат (по протоколам)
6. **Logging:** Добавлен раздел про отладку через logger
7. **Protocol Table:** Оформлена таблица поддержки Reality/Vision
8. **Safety:** Упомянуто "set_system_proxy": False как фича безопасности