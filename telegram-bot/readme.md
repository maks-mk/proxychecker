# 🛡️ Sing-box Proxy Checker Bot (v6.0 Secure)

Профессиональный Telegram-бот для проверки прокси.
Версия **v6.0** сочетает высокую скорость проверки (Turbo) с **Enterprise-защитой** от перегрузок, утечек памяти и спама. Идеален для публичного размещения.

![Python](https://img.shields.io/badge/Python-3.9%2B-blue) ![Sing-box](https://img.shields.io/badge/Core-v1.11.4-success) ![Security](https://img.shields.io/badge/Security-Enterprise-green)

## 🔥 Ключевые возможности

### 🚀 Производительность
*   **Turbo Engine:** Гибридная архитектура (Asyncio + ThreadPool) обрабатывает сотни прокси за секунды.
*   **Smart Deduplication:** Логическое удаление дублей (Host + Port + Auth).
*   **Link Hygiene:** Глубокая очистка ссылок от мусора (`name`, `spider`, `plugin`) перед проверкой.

### 🛡️ Безопасность и Стабильность (New!)
*   **Anti-Abuse System:** Лимиты на количество проверок в час и размер загружаемых файлов.
*   **Memory Safety:** Встроенный **Garbage Collector** автоматически очищает оперативную память от старых данных.
*   **Non-blocking I/O:** Полностью асинхронная работа с файлами через `aiofiles` — бот не зависает при загрузке больших баз.
*   **FloodWait Protection:** Умная очередь отправки сообщений, чтобы Telegram не банил бота за спам статусами.

## 📡 Поддерживаемые протоколы
*   **VLESS** (Reality, Vision, gRPC, WS, TCP)
*   **VMess** (WS, gRPC, TCP)
*   **Trojan**
*   **Shadowsocks** (Legacy & SIP002)
*   **Hysteria 2**

## 🛠 Установка

1.  **Требования:** Python 3.8+
2.  **Зависимости** (добавлен `aiofiles`):
    ```bash
    pip install aiogram aiohttp requests aiofiles
    ```

## ⚙️ Настройка

### 1. `bot.py` (Лимиты и защита)
```python
TOKEN = "YOUR_TOKEN_HERE"

# Anti-Abuse Настройки
MAX_FILES_PER_USER = 5       # Файлов в очереди на юзера
MAX_LINKS_PER_CHECK = 1000   # Ссылок за одну проверку
MAX_FILE_SIZE_MB = 5         # Макс. размер файла (защита от DoS)
CHECKS_PER_HOUR = 10         # Лимит проверок в час
DATA_LIFETIME_HOURS = 2      # Время жизни данных в RAM до очистки
```

### 2. `checker_engine.py` (Движок)
```python
TIMEOUT_SEC = 8              # Таймаут на подключение
GLOBAL_POOL = 60             # Кол-во потоков (50-60 оптимально для 1-2 vCPU)
BATCH_SIZE = 50              # Размер пачки для Sing-box
```

## ▶️ Запуск
```bash
python bot.py
```
*Ядро Sing-box скачивается автоматически при первом запуске.*

## 🤖 Использование

1.  Отправьте боту **.txt** файлы.
2.  Бот покажет количество уникальных строк и удалит мусор.
3.  Нажмите **🚀 Запустить проверку**.
4.  Наблюдайте за прогрессом в реальном времени (без фризов).
5.  Получите отчет `live_HH-MM.txt` с рабочими прокси.

**Формат вывода:**
```text
vless://uuid@ip:port?...#🇩🇪 DE | 🚀 125ms | VLESS
```

---
*License: MIT*