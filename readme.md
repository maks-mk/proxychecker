# 🚀 Sing-box Proxy Checker v5.7 (Turbo Stats+)

Высокопроизводительная консольная утилита для проверки прокси.
Оптимизирована для **максимальной скорости** даже на слабом железе (2 ядра, старые CPU).
Использует ядро **Sing-box v1.11.4**.

![Python](https://img.shields.io/badge/Python-3.8%2B-blue) ![Sing-box](https://img.shields.io/badge/Core-v1.11.4-success) ![Speed](https://img.shields.io/badge/Speed-Turbo-red)

## 🔥 Ключевые возможности

*   **Turbo Engine:** Пакетная обработка (Batching 100/cycle). Запускает ядро реже, но эффективнее. Скорость проверки выросла в 10+ раз.
*   **Smart Deduplication:** Удаляет дубликаты на логическом уровне (Host + Port + Auth).
*   **Link Hygiene:** Вырезает мусорные параметры (`name`, `spider`, `plugin`, `remarks`) перед проверкой.
*   **Extended Stats:** Детальный отчет в конце: медианный пинг, % быстрых прокси, ТОП стран, анализ ошибок (почему прокси умерли).
*   **TCP Pre-Check:** Мгновенный отсев недоступных портов перед тяжелой проверкой.
*   **IP Leak Protection:** Отбрасывает прозрачные прокси, не скрывающие ваш IP.

## 📡 Поддерживаемые протоколы
*   **VLESS** (Reality, Vision, gRPC, WS, TCP)
*   **VMess** (WS, gRPC, TCP)
*   **Trojan**
*   **Shadowsocks** (Legacy & SIP002)
*   **Hysteria 2**

## 🛠 Установка

1.  **Требования:** Python 3.8+
2.  **Зависимости:**
    ```bash
    pip install requests tqdm colorama
    ```

## 🚀 Использование

1.  Поместите файлы с прокси (`.txt`) в папку **`keys/`**.
2.  Запустите скрипт:
    ```bash
    python proxy_checker.py
    ```
    *(При первом запуске ядро Sing-box скачается автоматически)*.
3.  Заберите рабочие прокси в **`output/live.txt`**.

## ⚙️ Тонкая настройка (внутри скрипта)

```python
TIMEOUT_SEC = 8             # Таймаут (8 сек оптимально для медленных прокси, 3 сек для быстрых)
CHECK_IP_LEAK = True        # Проверка на утечку IP

# Для слабых ПК (i3/i5 старых поколений):
BATCH_SIZE = 100            # Большой батч снижает нагрузку на CPU
GLOBAL_POOL = 60            # Ограничение потоков во избежание фризов
```

## 📊 Пример статистики

```text
⏱  Время: 68.7s | Скорость: 4.6 prx/s
✅ Результат: 180/313 (57.5%)

🚀 ПИНГ И СКОРОСТЬ:
   • Мин: 341ms | Макс: 7535ms
   • Средний: 1825ms | Медиана: 1635ms
   • Быстрых (<500ms): 4 (2%)

🌍 ТОП-10 СТРАН:
   🇷🇺 RU  : 30
   🇳🇱 NL  : 22

❌ АНАЛИЗ ОШИБОК:
   • SSL Error      : 111 (Порт открыт, но шифрование не прошло)
   • TCP Closed     : 8
```

---
*License: MIT*